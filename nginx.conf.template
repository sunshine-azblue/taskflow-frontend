server {
    listen 80;

    location /api/ {
        proxy_pass http://${BACKEND_HOST}:${BACKEND_PORT}/api/;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
